# Lightweight confidence findings — 2026-09-16

**Result: no live scenario passed.** Four cases ran once and failed their acceptance oracles; explicit parallel was blocked by the real private-adapter reservation preflight and received zero paid requests. The distinct `lightweight-confidence-v1` campaign consumed **19 of 32 authorized Luna physical requests**, reporting **18,251 tokens across four roots**. All 19 transport outcomes were HTTP success; no ambiguous send, transport retry, fallback model, or additional live case invocation occurred. The 13 unused calls remain unused and cannot be transferred or used to retry these terminal cases.

## Scope and accounting

Execution used **gpt-6-astra / medium**, verified from the [execution header](../var/acceptance/lightweight-confidence-v1/execution-header.txt). No additional agents were used. Live requests used the existing authorized key, official `https://api.openai.com/v1/responses`, `gpt-5.6-luna`, reasoning `none`; output caps were 1,024 for bug/CSV/recovery and 768 for direct/parallel. Each root retained the production 16,000-token ceiling and existing completion/security gates. No runtime behavior, production configuration, API schema, or deployment was changed.

The immutable [new ledger](../var/acceptance/lightweight-confidence-v1.sqlite), [started marker](../var/acceptance/lightweight-confidence-v1.started), and [read-only accounting export](../var/acceptance/lightweight-confidence-v1/ledger-evidence.json) contain four admitted root IDs, all physical sends/outcomes, and five terminal latches. Child/retry sends share the same scenario allocation; the transport counts before sending, buffers response bodies to detect ambiguous reads, and reconciles unrecorded outcomes before worker restart. CLI reruns cannot resubmit this live campaign. Task proof comes from authenticated public run/task/budget/operations/events/children/verifications and file downloads, not ledger inspection.

| Case | Outcome | Calls/cap | Reported tokens | Root ID | Proof |
|---|---|---:|---:|---|---|
| bug | failed | 5/5 | 4,610 | `624cf595-21fc-4b06-97b3-222d593b3f29` | [case evidence](../var/acceptance/lightweight-confidence-v1/bug-live.json) |
| csv | failed | 6/6 | 6,242 | `dfa2b268-1f88-4962-8b29-08bf733780c6` | [case evidence](../var/acceptance/lightweight-confidence-v1/csv-live.json) |
| recovery | failed | 5/6 | 4,684 | `5125a72b-bc80-4d09-a42e-71532b32a25a` | [case evidence](../var/acceptance/lightweight-confidence-v1/recovery-live.json) |
| direct | failed | 3/3 | 2,715 | `c5f105f5-2e41-4623-bde1-4f3ce0286c9e` | [case evidence](../var/acceptance/lightweight-confidence-v1/direct-live.json) |
| parallel | blocked | 0/12 | 0 | `not submitted` | [case evidence](../var/acceptance/lightweight-confidence-v1/parallel-live.json) |

## Observed actions and confidence

1. **Bug fix — failed completion, correct retained edit.** Actions were read `main.py`, read `test_main.py`, read `main.py` again, write the corrected function, then request verification in the final reporting slot. The runtime replaced that last non-report action with `blocked`: “Final reporting reserve reached; work remains incomplete.” The downloaded source is `def double(x):` followed by `return x * 2`; independent AST evaluation gives 8, -6, and 0 for 4, -3, and 0. The downloaded supplied test bytes/hash match the upload. There are no fresh pytest or syntax receipts, so this is evidence of a correct edit on this tiny fixture, **not a completed, verified bug-fix run**. [Source](../var/acceptance/lightweight-confidence-v1/live-downloads/624cf595-21fc-4b06-97b3-222d593b3f29/main.py), [unchanged tests](../var/acceptance/lightweight-confidence-v1/live-downloads/624cf595-21fc-4b06-97b3-222d593b3f29/test_main.py).

2. **Messy CSV — failed output and completion.** Luna read the input twice, wrote both outputs, then read the input twice more. Its final action hit the reporting reserve. `cleaned.csv` independently matches exactly `name,amount\nAlice,7\nBob,-2\nEve,9\n`, and the input is unchanged. `summary.csv` is `metric,value\nvalid_rows,3\n`, rather than the registered exact output `valid_rows,rejected_rows,total\n3,2,14\n`. No fresh byte-check or syntax receipts were produced. This supports correct trimming/filtering/order on this one input, but **does not support correct summary generation or autonomous completion**. The short prompt named the two output files; the exact summary schema was supplied in the registered check, not separately spelled out in prose. This is a limitation of the prompt/check interface being exercised, not evidence that all CSV tasks fail. [Cleaned bytes](../var/acceptance/lightweight-confidence-v1/live-downloads/dfa2b268-1f88-4962-8b29-08bf733780c6/cleaned.csv), [actual summary](../var/acceptance/lightweight-confidence-v1/live-downloads/dfa2b268-1f88-4962-8b29-08bf733780c6/summary.csv), [unchanged input](../var/acceptance/lightweight-confidence-v1/live-downloads/dfa2b268-1f88-4962-8b29-08bf733780c6/input.csv).

3. **Command recovery — failed diagnosis, stopped for no progress.** The first `python report.py input.csv` correctly failed with a downloaded missing-file diagnostic. This expected failure was part of the task. Luna then chose that same wrong command on every remaining model decision, never the required corrected `python report.py inputs.csv`. The run stopped at `no_progress` after five requests, before its six-call cap. `result.csv` is absent; supplied script and input hashes are unchanged. This proves observable command failure and a bounded no-progress stop, **not recovery from that failure**. The repeated commands were actions within the same root, not retried campaign invocations. [Initial diagnostic](../var/acceptance/lightweight-confidence-v1/live-downloads/5125a72b-bc80-4d09-a42e-71532b32a25a/5125a72b-bc80-4d09-a42e-71532b32a25a_action_0.stderr), [unchanged script](../var/acceptance/lightweight-confidence-v1/live-downloads/5125a72b-bc80-4d09-a42e-71532b32a25a/report.py), [unchanged input](../var/acceptance/lightweight-confidence-v1/live-downloads/5125a72b-bc80-4d09-a42e-71532b32a25a/inputs.csv).

4. **Simple direct — failed bytes and completion, no delegation.** Luna first wrote the correct `12\n`, then overwrote it with `7\n12\n`; its third write proposal hit the final-report reserve. The final authenticated download is wrong and lacks fresh byte/syntax receipts. Zero children, assignments, and merges were observed despite delegation being available. This supports the narrow claim that delegation was optional in this run; **it does not establish reliable direct-file completion**. [Final bytes](../var/acceptance/lightweight-confidence-v1/live-downloads/c5f105f5-2e41-4623-bde1-4f3ce0286c9e/sum.txt).

5. **Explicit parallel — blocked before paid submission.** The exact fixture ran through the real SDK, private semantic adapter, runtime accounting, actual child workflows, branch grants, and merge path with an offline Responses transport. One assignment created two overlapping child lifetimes with disjoint `left.txt` / `right.txt` output grants. Each child wrote its own correct file; two merges produced exact integrated parent bytes, with no parent-authored substitute writes. However, neither child could reserve completion, and neither child nor parent obtained the required fresh checks. The live case therefore has no root and zero paid calls. This provides evidence of branch creation, scoped writes, overlap, and merging under a deterministic stub, **not successful live delegation or verified parallel completion**.

## Reservation preflight

The [final preflight report](../var/acceptance/lightweight-confidence-v1/preflight-gate.json) preserves HTTP evidence and exact request-reservation measurements. Four nonparallel cases passed their full oracles via the real SDK and private adapter, using 3/3/4/2 mock sends. Parallel used six mock sends and was blocked. These are offline HTTP transport invocations, not additional Luna requests. Stub token usage is a nonzero deterministic byte-based estimate, not measured provider billing.

Context bytes grew as follows: bug 5,559→5,987; CSV 5,753→6,213; recovery 5,740→6,353; direct 5,467→5,907. Reservation uses context bytes + output cap + 512. Two children received server-derived local allocations of 6,208 tokens each after the existing 3,584-token reserve. Left completion needed 5,874 reserved tokens plus 1,005 already reported = 6,879; right needed 5,880 + 1,008 = 6,888. Both exceed 6,208. The eventual parent completion needed 8,494 + 8,493 reported = 16,987, exceeding 16,000. No ceilings or allocations were raised to force a pass. This is a concrete failure of this fixed replay under current accounting, not a claim about every possible parallel strategy.

Preflight parent root: `ef002dc3-5012-4c76-b2c3-4ab89c53b100`; children: `3ff81a8f-1d7a-503a-b295-68439a646fe7` (left), `61b029c4-a4cf-5dbf-af6a-b24d1b57e8c1` (right). The preflight report contains their public scopes, operations, receipts, events, and integrated downloads.

## Validation, retained failures, and preservation

- [54 bounded offline tests passed](../var/acceptance/lightweight-confidence-v1/offline-regressions.log), covering new/legacy budgets, immutable identity, concurrent cap exhaustion, failure/unknown-outcome latches, restart reconciliation, transport body failures, adapter contracts, semantic behavior, and negative oracle checks for stale receipts/changed inputs.
- [Service suite: 49 passed, one timeout](../var/acceptance/lightweight-confidence-v1/service-regressions.log). Tests covered actual commands, children, sparse grants/conflicts, integrity/stale checks, security/isolation, cancellation, worker/broker recovery, replay, and migration. The suite was paused to prevent overlap with campaign preflight broker use; the child-allocation test subsequently timed out. Its [isolated rerun passed](../var/acceptance/lightweight-confidence-v1/child-regression-rerun.log). The timeout is retained, not erased or counted as a first-run pass.
- `uv run ruff check .`, `uv run ruff format --check .`, and `git diff --check` passed. Existing dirty user work and `AGENTS.md` were preserved. Only acceptance additions, narrow existing transport/worker hooks, and requested documentation were edited.
- Initial no-paid preflight found two acceptance-harness issues: recovery command/result linkage used the wrong public shape; parallel lifecycle inspection used a nonexistent timestamp field. Both were corrected. Its cleanup query also incorrectly assumed a `kind` SQL column; the same query issue affected the second preflight. Both isolated schemas were explicitly reconciled/exported and removed. All original evidence and failures remain; the final preflight completed cleanup normally. [Initial report](../var/acceptance/lightweight-confidence-v1/preflight.json), [first reconciliation](../var/acceptance/lightweight-confidence-v1/preflight-initial-reconciled.json), [second report](../var/acceptance/lightweight-confidence-v1/preflight-44a1c80621834a8993b0249a0b4836b2.json), [second reconciled report](../var/acceptance/lightweight-confidence-v1/preflight-second-reconciled.json), [final execution log](../var/acceptance/lightweight-confidence-v1/preflight-final.log).
- API/worker fixtures used unique schemas, queues, and ephemeral loopback API ports; commands used the separate test broker on 18091. Fault injection was confined to test workers and that owned broker. Deployment 18000, unrelated 8000, existing application data, and service volumes were not changed. Every confidence-campaign fixture schema was removed.
- Historical ledgers and markers remain byte-for-byte unchanged: v3 **19/24**, v2 **48/50**, historical **16/20**. [Before hashes](../var/acceptance/lightweight-confidence-v1/preserved-before.json) and [after hashes](../var/acceptance/lightweight-confidence-v1/preserved-after.json) also confirm `AGENTS.md` unchanged. No old campaign was reset, relocated, restored, or consumed.

The campaign demonstrates enforcement of caps and failure gates, plus several partial artifacts. It does **not** establish lightweight autonomous-task reliability or deployment readiness. Findings are complete; no open-ended runtime fixing, additional paid testing, or deployment was initiated.
