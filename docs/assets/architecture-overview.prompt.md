# Architecture illustration

Generated with the built-in image-generation tool. This is a conceptual overview; the README and public contracts describe supported behavior.

## Generation prompt

Use case: infographic-diagram. Create a polished, technically accurate architecture and process illustration for the GitHub README of "Independent Agents API". Wide landscape high-resolution canvas, white/light neutral background, dark navy readable typography, restrained teal and blue accents, elegant flat engineering editorial style. No 3D, no robots, no stock imagery, no brand logos, no decorative circuit noise. Generous whitespace, crisp aligned rounded rectangles, clear arrowheads, large readable labels at README width. Title: "Independent Agents API"; subtitle: "Durable execution. Authorized tools. Verified results."
Main area labeled "SYSTEM ARCHITECTURE": clean left-to-right layout of 4 main cards: "Clients" (small line: "HTTP · Python · CLI") -> "Public API" (small line: "FastAPI · Auth · Run control") -> "Durable workflows" (small line: "Temporal · Recovery · Coordination") -> "Activities" (small line: "Model calls · Tool execution"). Place below Public API a database-shaped card "PostgreSQL" with small line "State · Events · Outbox" and bidirectional connector API/database. Connector from database towards workflows labeled "Outbox dispatch". API/workflow arrow represents run control. Under Activities place two neatly separated cards "Model adapters" (small line "PydanticAI · Explicit provider") and "Authorized tools" (small line "Permissions · Approvals"), both connected bidirectionally to Activities. Under Authorized tools, a clearly outlined boundary "Sandbox broker" with inner card "Isolated containers" and small line "Offline Python · Immutable revisions"; connect tools to broker, broker to containers. Under Durable workflows place "Optional subagents" (small line "Scoped tasks · Shared limits") with bidirectional workflow connector; don't connect subagents directly to modelproviders. Arrows must have sensible minimal routing with no crossings over text. All execution model/tool I/O goes through Activities, never show workflow directly calling modelprovider.
Bottom horizontal band separated by whitespace labeled "TASK LIFECYCLE": "Request + context" -> "Plan" -> "Act / delegate" -> "Verify" -> "Result"; draw a curved return arrow from Verify to Plan labeled "Revise when needed". Small supporting footer beneath "Persisted events expose progress through the API". This is a conceptual overview, not a promise every request succeeds; avoid "always succeeds" or production claims. Render all labels exactly and legibly; no additional invented components. Prioritize coherent accurate graph and tasteful design over density.
